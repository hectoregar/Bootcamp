export default function ContacPage() {
    const title = <h2> Contactenos</h2>;
    const mission = <p> Estamos a su disposición en que le podemos apoyar </p>
        return (
            <section>
                {title}
                {mission}
            </section>
        )
}